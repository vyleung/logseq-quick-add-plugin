{
  "name": "logseq-quick-add-plugin",
  "version": "1.1.1",
  "description": "Add and duplicate blocks using keyboard shortcuts",
  "main": "dist/index.html",
  "targets": {
    "main": false
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-source-maps src/index.html --public-url ./"
  },
  "keywords": [],
  "author": "vyleung",
  "license": "MIT",
  "dependencies": {
    "@logseq/libs": "^0.0.6",
    "date-fns": "^2.28.0",
    "dayjs": "^1.11.2",
    "driftless": "^2.0.3"
  },
  "devDependencies": {
    "parcel": "^2.5.0"
  },
  "logseq": {
    "id": "logseq-quick-add-plugin",
    "title": "Quick Add",
    "icon": "./icon.png"
  }
}
